services:
  - type: web
    name: notion-cloud-drive
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python notion_server.py
    envVars:
      - key: NOTION_TOKEN
        sync: false
      - key: NOTION_DATABASE_ID
        sync: false
